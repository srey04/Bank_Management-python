DATABASES TO BE ADDED:
1.BANK_MANAGEMENT 
TABLES TO BE ADDED:
1.Account_Number 
2.TRANSACTIONS

Queries to create:
1. Account_Number
2. TRANSACTIONS

#To create a new table TRANSANCTIONS in BANK_MANAGEMENT database:
USE BANK_MANAGEMENT;
CREATE TABLE TRANSANCTIONS(ACCOUNT_NO char(16) primary key,AMOUNT integer,TIME datetime);

#pls refer to this before the above one:
CREATE DATABASE IF NOT EXISTS BANK_MANAGEMENT;
USE BANK_MANAGEMENT;
CREATE TABLE Account_Number(ACCOUNT_NO CHAR(16) PRIMARY KEY ,NAME CHAR(20),PHONE_NO CHAR(10),ACCOUNT_TYPE CHAR(10),BALANCE INTEGER,PASSWORD CHAR(4));

